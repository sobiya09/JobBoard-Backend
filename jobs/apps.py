from django.apps import AppConfig

class JobsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'jobs'  # This should match the app folder name

# class ContactConfig(AppConfig):
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'contact'
